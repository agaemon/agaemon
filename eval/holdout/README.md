# Holdout Inputs

This directory contains holdout inputs only. Holdout answers live outside the
repo and are supplied to `harness/score.sh --holdout` by the harness owner.

Do not add answers, expected strings, canaries, or scoring thresholds here.

